---
phase: 04-framework
plan: 05
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - src/embedagent/query_engine.py
  - src/embedagent/strategies/turn_orchestrator.py
  - src/embedagent/strategies/execution_tracer.py
  - src/embedagent/strategies/__init__.py
  - docs/agent-state-machine.md
  - tests/test_execution_tracer.py
autonomous: true
requirements:
  - FRAM-05
  - FRAM-06

must_haves:
  truths:
    - "Agent loop emits structured execution traces for every turn"
    - "Traces capture: turn start, LLM call, tool execution, errors, state transitions"
    - "State machine documentation describes all states and transitions explicitly"
  artifacts:
    - path: "src/embedagent/strategies/execution_tracer.py"
      provides: "ExecutionTracer with structured trace events"
      exports: ["ExecutionTracer", "TraceEvent"]
    - path: "src/embedagent/query_engine.py"
      provides: "QueryEngine integrated with tracer"
    - path: "docs/agent-state-machine.md"
      provides: "Explicit state machine documentation"
      contains: "state diagram"
  key_links:
    - from: "QueryEngine.submit_user_turn()"
      to: "ExecutionTracer"
      via: "trace events at turn boundaries"
      pattern: "tracer.record"
    - from: "TurnOrchestrator.execute_turn()"
      to: "ExecutionTracer"
      via: "trace events for LLM and tool phases"
      pattern: "tracer.record"
---

<objective>
Add structured logging, execution tracing, and explicit state machine documentation to the agent loop.

Purpose: Improve debuggability and observability of agent execution by capturing structured traces of every turn, and provide clear documentation of the agent state machine for developers.
Output: ExecutionTracer integrated into QueryEngine and TurnOrchestrator, plus comprehensive state machine documentation.
</objective>

<execution_context>
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/embedagent/query_engine.py
@src/embedagent/strategies/turn_orchestrator.py
@src/embedagent/strategies/llm_retry_wrapper.py
</context>

<interfaces>
From src/embedagent/query_engine.py:
- `QueryEngine.submit_user_turn(user_text, stream, initial_mode, workflow_state, session, stop_event)` -> QueryTurnResult
- Key phases: initialize_session, harness mode setup, turn loop (while max_turns), transcript events
- Uses `TurnOrchestrator.execute_turn()` for each turn

From src/embedagent/strategies/turn_orchestrator.py:
- `TurnOrchestrator.execute_turn(session, messages, tool_schemas, current_mode, workflow_state, stream, stop_event, ...)` -> QueryTurnResult
- Phases: LLM call, tool action execution (serial/parallel), permission handling, loop guard checks
- Returns `QueryTurnResult(content, session, transition)`

From src/embedagent/strategies/llm_retry_wrapper.py:
- `LLMClientRetryWrapper.call_with_retry(messages, tools, stream, ...)` -> AssistantReply
- Has retry attempts, context compaction events

From src/embedagent/session.py:
- `QueryTurnResult(content, session, transition, pending_interaction)`
- `LoopTransition(reason, message, next_mode, pending_interaction)`
- Reasons: "completed", "error", "tool_calls", "aborted", "guard_stop", "permission_wait"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ExecutionTracer with structured trace events</name>
  <files>src/embedagent/strategies/execution_tracer.py, src/embedagent/strategies/__init__.py</files>
  <action>
Create `src/embedagent/strategies/execution_tracer.py` for structured execution tracing:

**Enum: TraceEventType**
- `TURN_START = "turn_start"`
- `LLM_CALL_START = "llm_call_start"`
- `LLM_CALL_END = "llm_call_end"`
- `LLM_RETRY = "llm_retry"`
- `TOOL_EXECUTION_START = "tool_execution_start"`
- `TOOL_EXECUTION_END = "tool_execution_end"`
- `TOOL_PARALLEL_BATCH = "tool_parallel_batch"`
- `PERMISSION_REQUEST = "permission_request"`
- `PERMISSION_DECISION = "permission_decision"`
- `STATE_TRANSITION = "state_transition"`
- `CHECKPOINT_SUSPEND = "checkpoint_suspend"`
- `CHECKPOINT_RESUME = "checkpoint_resume"`
- `TURN_END = "turn_end"`
- `ERROR = "error"`

**Dataclass: TraceEvent**
- `event_type: TraceEventType`
- `timestamp: float` (time.time())
- `turn_id: str`
- `step_id: Optional[str]`
- `session_id: str`
- `data: Dict[str, Any]` (event-specific payload)
- `duration_ms: Optional[int]` = None

**Class: ExecutionTracer**

Constructor:
- `__init__(self, output_dir: Optional[str] = None)`
- If output_dir provided, traces are flushed to JSONL files
- Otherwise, kept in memory

Methods:

1. `record(self, event_type: TraceEventType, session_id: str, turn_id: str, step_id: Optional[str] = None, data: Optional[Dict[str, Any]] = None, duration_ms: Optional[int] = None) -> TraceEvent`:
   - Create TraceEvent
   - Append to in-memory buffer
   - If buffer > 100 events, flush to disk
   - Return event

2. `start_span(self, event_type: TraceEventType, session_id: str, turn_id: str, step_id: Optional[str] = None, data: Optional[Dict[str, Any]] = None) -> _TraceSpan`:
   - Return context manager that records start event and end event with duration
   - Usage: `with tracer.start_span(TraceEventType.LLM_CALL_START, ...): ...`

3. `flush(self) -> None`:
   - Write buffered events to `{output_dir}/traces/{session_id}/{date}.jsonl`
   - Clear buffer

4. `get_traces(self, session_id: Optional[str] = None, turn_id: Optional[str] = None) -> List[TraceEvent]`:
   - Filter and return matching traces

5. `summary(self, session_id: str) -> Dict[str, Any]`:
   - Return aggregate stats: turn_count, total_tool_calls, total_llm_calls, error_count, avg_turn_duration_ms

**Internal: _TraceSpan context manager**
- Records start event on enter
- Records end event with duration on exit
- Captures exceptions and records ERROR event

**Python 3.8 constraints:**
- Use `typing.List`, `typing.Dict`, `typing.Optional`, `typing.Any`
- Use `contextlib.contextmanager` or manual context manager class
- Use `json.dumps` with `default=str` for serialization
- Use `datetime.now(timezone.utc).isoformat()` for timestamps in filenames

**Update `src/embedagent/strategies/__init__.py`:**
- Add exports for `ExecutionTracer`, `TraceEvent`, `TraceEventType`
  </action>
  <verify>
    <automated>python -c "from embedagent.strategies.execution_tracer import ExecutionTracer, TraceEventType; t = ExecutionTracer(); print('import ok')"</automated>
  </verify>
  <done>ExecutionTracer exists with record/start_span/flush/get_traces/summary methods; TraceEventType has 14 event types; _TraceSpan context manager works</done>
</task>

<task type="auto">
  <name>Task 2: Integrate tracer into QueryEngine and TurnOrchestrator</name>
  <files>src/embedagent/query_engine.py, src/embedagent/strategies/turn_orchestrator.py</files>
  <action>
Integrate ExecutionTracer into the agent loop to capture structured execution traces.

**In `src/embedagent/query_engine.py`:**

1. **Add tracer to QueryEngine:**
   - Import `ExecutionTracer` from `.strategies.execution_tracer`
   - Add optional `tracer` parameter to `__init__` (default `None`)
   - Store as `self._tracer`
   - If not provided, create `ExecutionTracer()` with no output (in-memory only)

2. **Instrument `submit_user_turn()`:**
   - At turn start: `tracer.record(TraceEventType.TURN_START, ...)` with mode, workflow_state
   - Before LLM call: use `tracer.start_span(TraceEventType.LLM_CALL_START, ...)` around the turn execution
   - On turn end: `tracer.record(TraceEventType.TURN_END, ...)` with transition reason
   - On error: `tracer.record(TraceEventType.ERROR, ...)` with exception info
   - Before returning: `tracer.flush()`

3. **Instrument `run()`:**
   - Record TURN_START at entry
   - Record TURN_END at exit with total duration

4. **Instrument initialization:**
   - Record checkpoint events when session is initialized

**In `src/embedagent/strategies/turn_orchestrator.py`:**

1. **Add tracer to TurnOrchestrator:**
   - Import `ExecutionTracer`, `TraceEventType`
   - Add optional `tracer` parameter to `__init__` (default `None`)

2. **Instrument `execute_turn()`:**
   - LLM call phase: wrap `self.llm_wrapper.call_with_retry()` in `tracer.start_span(TraceEventType.LLM_CALL_START, ...)`
   - On LLM success: record LLM_CALL_END with action count, finish_reason
   - Tool execution: for each action, record TOOL_EXECUTION_START/END with tool name, success status
   - Parallel batches: record TOOL_PARALLEL_BATCH with batch size
   - Permission decisions: record PERMISSION_REQUEST/DECISION with tool name and decision
   - State transitions: record STATE_TRANSITION with old_mode, new_mode, reason
   - On suspend (pending interaction): record CHECKPOINT_SUSPEND
   - On resume: record CHECKPOINT_RESUME

3. **Instrument error paths:**
   - On ModelClientError: record ERROR with error type and message
   - On ToolError: record ERROR in tool execution result
   - On LoopGuard stop: record STATE_TRANSITION with guard_stop reason

**Python 3.8 constraints:**
- Tracer is optional: all instrumented code must handle `tracer is None` gracefully
- No changes to public API signatures (tracer parameter is optional keyword arg)
- Maintain backward compatibility: existing callers without tracer continue to work
  </action>
  <verify>
    <automated>grep -n "ExecutionTracer" src/embedagent/query_engine.py && grep -n "TraceEventType" src/embedagent/strategies/turn_orchestrator.py | head -10</automated>
  </verify>
  <done>QueryEngine and TurnOrchestrator both accept optional tracer parameter; key execution phases emit trace events; tracer is None-safe</done>
</task>

<task type="auto">
  <name>Task 3: Create state machine documentation and tracer tests</name>
  <files>docs/agent-state-machine.md, tests/test_execution_tracer.py</files>
  <action>
Create state machine documentation and comprehensive tracer tests.

**In `docs/agent-state-machine.md`:**

Create explicit state machine documentation:

```markdown
# Agent Execution State Machine

## States

### Session States
- `UNINITIALIZED` — Session not yet set up
- `INITIALIZED` — Session ready, mode loaded
- `TURN_ACTIVE` — Processing a user turn
- `WAITING_PERMISSION` — Blocked on user permission
- `WAITING_INPUT` — Blocked on user input (ask_user)
- `COMPACTING` — Context compaction in progress
- `ERROR` — Fatal error encountered
- `STOPPED` — Stop signal received

### Turn States (within TURN_ACTIVE)
- `TURN_START` — Turn begun
- `LLM_CALLING` — Awaiting LLM response
- `TOOL_EXECUTING` — Running tool actions
- `PERMISSION_CHECKING` — Evaluating permission policy
- `TURN_END` — Turn completed

## Transitions

| From | Event | To | Description |
|------|-------|-----|-------------|
| UNINITIALIZED | initialize_session | INITIALIZED | Mode loaded, harness context injected |
| INITIALIZED | submit_user_turn | TURN_ACTIVE | User input received |
| TURN_ACTIVE | call_llm | LLM_CALLING | Sending prompt to LLM |
| LLM_CALLING | llm_reply | TOOL_EXECUTING | Got response with tool calls |
| LLM_CALLING | llm_completed | TURN_END | Got response without tool calls |
| TOOL_EXECUTING | all_tools_done | TURN_END | All actions executed |
| TOOL_EXECUTING | permission_needed | WAITING_PERMISSION | Action requires approval |
| WAITING_PERMISSION | approved | TOOL_EXECUTING | User approved, resume |
| WAITING_PERMISSION | denied | TURN_END | User denied, turn ends |
| TURN_ACTIVE | checkpoint_suspend | WAITING_INPUT | ask_user triggered |
| WAITING_INPUT | user_responds | TURN_ACTIVE | User provided input |
| LLM_CALLING | context_length_error | COMPACTING | Prompt too long |
| COMPACTING | compacted | LLM_CALLING | Retry with compacted context |
| TURN_ACTIVE | stop_signal | STOPPED | User cancelled |
| TURN_ACTIVE | error | ERROR | Unrecoverable error |

## Error Handling

### Retryable Errors
- HTTP 429, 500, 502, 503, 504 — LLM retry with backoff
- Context length — Compaction and retry

### Non-Retryable Errors
- HTTP 400, 401, 403 — Fail immediately
- Tool validation errors — Return to LLM as observation
- Permission denied — Return as observation

## Loop Guard

The LoopGuard monitors for:
- Repeated identical tool calls (same name + arguments)
- Excessive turn count (> max_turns)
- Circular state transitions
```

**In `tests/test_execution_tracer.py`:**

Create comprehensive tests:

**Test class: TestExecutionTracer**

1. `test_record_creates_trace_event`:
   - Record a TURN_START event
   - Assert returned TraceEvent has correct type, timestamp, session_id

2. `test_start_span_records_duration`:
   - Use context manager with 0.1s sleep
   - Assert start and end events recorded
   - Assert duration_ms >= 100

3. `test_span_captures_exception`:
   - Use context manager that raises RuntimeError
   - Assert ERROR event recorded with exception info
   - Assert end event still recorded

4. `test_flush_writes_to_disk`:
   - Create tracer with temp output_dir
   - Record 2 events
   - Call flush()
   - Assert JSONL file exists with 2 lines

5. `test_get_traces_filters_by_session`:
   - Record events for session_1 and session_2
   - Get traces for session_1
   - Assert only session_1 events returned

6. `test_summary_aggregates_stats`:
   - Record: 2 turns, 3 tool calls, 1 error
   - Call summary("session_1")
   - Assert stats match recorded events

7. `test_integration_in_query_engine`:
   - Mock QueryEngine with tracer
   - Simulate a turn
   - Assert tracer has TURN_START, LLM_CALL_START, TURN_END events

8. `test_integration_in_turn_orchestrator`:
   - Mock TurnOrchestrator with tracer
   - Simulate tool execution
   - Assert TOOL_EXECUTION_START and TOOL_EXECUTION_END events

**Run tests:**
`uv run pytest tests/test_execution_tracer.py -v`
  </action>
  <verify>
    <automated>uv run pytest tests/test_execution_tracer.py -v</automated>
  </verify>
  <done>8 tests pass; state machine doc has 8 session states, 6 turn states, 15+ transitions; tracer integration verified in QueryEngine and TurnOrchestrator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent loop -> Trace output | Execution traces may contain sensitive argument data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | Trace files | mitigate | Sanitize tool arguments in trace data (remove file paths, API keys); store traces in `.embedagent/` which is gitignored |
| T-04-14 | Denial of Service | Trace buffer growth | mitigate | Auto-flush at 100 events; max file size 10MB |
| T-04-15 | Tampering | Trace file modification | accept | Traces are diagnostic, not security audit logs |
</threat_model>

<verification>
- [ ] `uv run pytest tests/test_execution_tracer.py -v` passes all tests
- [ ] `docs/agent-state-machine.md` exists with states, transitions, error handling
- [ ] QueryEngine records TURN_START/END events
- [ ] TurnOrchestrator records LLM and tool execution events
- [ ] Trace flush produces valid JSONL files
- [ ] No regression in existing test suite
</verification>

<success_criteria>
- ExecutionTracer exists with 14 event types and span tracking
- QueryEngine and TurnOrchestrator instrumented with trace events
- Tracer supports in-memory and disk-based output
- State machine documentation has explicit states, transitions, error handling
- 8+ characterization tests passing
- Trace summary provides aggregate stats (turn count, tool calls, errors)
- No regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-framework/04-05-SUMMARY.md`
</output>
