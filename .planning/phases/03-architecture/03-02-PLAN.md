---
phase: 03-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/embedagent/query_engine.py
  - src/embedagent/strategies/llm_retry_wrapper.py
  - src/embedagent/strategies/context_compaction_engine.py
  - src/embedagent/strategies/turn_orchestrator.py
  - src/embedagent/strategies/__init__.py
  - tests/test_strategies.py
  - tests/test_query_engine_orchestrator.py
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "QueryEngine is under 500 lines (down from 1,530)"
    - "Three strategy modules exist with clear single responsibilities"
    - "All existing tests pass without modification"
    - "New characterization tests verify strategy behavior"
  artifacts:
    - path: "src/embedagent/strategies/llm_retry_wrapper.py"
      provides: "LLM call retry with backoff and context-compaction fallback"
      min_lines: 80
    - path: "src/embedagent/strategies/context_compaction_engine.py"
      provides: "Context budget enforcement and compaction pipeline"
      min_lines: 80
    - path: "src/embedagent/strategies/turn_orchestrator.py"
      provides: "Single turn execution: prompt -> LLM -> tool actions -> observations"
      min_lines: 100
    - path: "src/embedagent/strategies/__init__.py"
      provides: "Strategy exports"
    - path: "src/embedagent/query_engine.py"
      provides: "Thin orchestrator delegating to strategies"
      max_lines: 500
  key_links:
    - from: "src/embedagent/query_engine.py"
      to: "src/embedagent/strategies/*"
      via: "Constructor injection"
      pattern: "from embedagent.strategies import"
---

<objective>
Extract QueryEngine (1,530 lines) into three focused strategies behind a thin orchestrator.

Purpose: Reduce complexity, improve testability, and separate LLM retry, context compaction, and turn execution concerns
Output: Four new/modified files — three strategy modules + refactored orchestrator
</objective>

<execution_context>
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/research/SUMMARY.md

Key patterns from codebase:
- QueryEngine at 1,530 lines handles: LLM retries with backoff, context compaction, tool partitioning, session persistence, memory maintenance, reasoning delta streaming
- Retry loop at line ~1,430 uses `# type: ignore[misc]` for `last_exc` possibly unbound
- Constructor takes: client, tools, max_turns, permission_policy, context_manager, summary_store, project_memory_store, memory_maintenance, intelligence_broker, max_parallel_tools, transcript_store
- Must maintain backward compatibility — other modules instantiate QueryEngine directly
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract LLMClientRetryWrapper strategy</name>
  <files>
    src/embedagent/strategies/llm_retry_wrapper.py
    src/embedagent/query_engine.py
    tests/test_strategies.py
  </files>
  <behavior>
    - Test: call_with_retry() succeeds on first attempt
    - Test: call_with_retry() retries on 429/500/502/503/504 and succeeds
    - Test: call_with_retry() triggers context compaction on "context length" error
    - Test: call_with_retry() raises after max retries exhausted
  </behavior>
  <action>
Create `src/embedagent/strategies/llm_retry_wrapper.py` containing:

1. **Class `LLMClientRetryWrapper`** with constructor accepting:
   - `client: OpenAICompatibleClient`
   - `max_retries: int = 3`
   - `base_delay: float = 1.0`
   - `compaction_engine: Optional[ContextCompactionEngine] = None`

2. **Methods extracted from QueryEngine:**
   - `call_with_retry(messages: List[Dict[str, Any]], tools: List[Dict[str, Any]], stream: bool = False, **kwargs) -> Dict[str, Any]` — the retry loop from lines ~1430-1500
   - `_should_retry_on_error(error: Exception) -> bool` — HTTP status code check
   - `_is_context_length_error(error_message: str) -> bool` — check against `_COMPACT_RETRY_ERROR_MARKERS`

3. **Implementation details:**
   - Keep `_RETRYABLE_HTTP_CODES = {429, 500, 502, 503, 504}`
   - Keep `_LLM_MAX_RETRIES = 3` and `_LLM_RETRY_BASE_DELAY = 1.0`
   - Keep `_COMPACT_RETRY_ERROR_MARKERS` tuple exactly as in query_engine.py
   - On context-length error: call `compaction_engine.compact(messages)` if available, then retry
   - Fix `# type: ignore[misc]` issue by initializing `last_exc: Optional[Exception] = None` before loop
   - Use exponential backoff with jitter: `delay = base_delay * (2 ** attempt) + random.uniform(0, 0.5)`
   - Log each retry attempt at WARNING level with attempt count and error

4. **Update QueryEngine:**
   - Add import: `from embedagent.strategies.llm_retry_wrapper import LLMClientRetryWrapper`
   - In `__init__`, instantiate: `self._llm_wrapper = LLMClientRetryWrapper(client, ...)`
   - Replace the retry loop in `_execute_turn` with: `response = self._llm_wrapper.call_with_retry(messages, tools, stream=True)`

5. **Tests in tests/test_strategies.py`:**
   - `test_call_succeeds_first_try`: mock client.chat.completions.create returns success
   - `test_retry_on_server_error`: mock returns 500 twice then success
   - `test_context_compaction_triggered`: mock returns "context length" error, verify compaction_engine.compact called
   - `test_max_retries_exhausted`: mock always returns 500, verify ModelClientError raised

Run tests: `uv run pytest tests/test_strategies.py::TestLLMClientRetryWrapper -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_strategies.py::TestLLMClientRetryWrapper -v</automated>
  </verify>
  <done>
    - LLMClientRetryWrapper exists at src/embedagent/strategies/llm_retry_wrapper.py
    - QueryEngine._execute_turn delegates to LLMClientRetryWrapper
    - All 4 characterization tests pass
    - Existing test suite still passes: uv run pytest tests/ -m "not slow and not gui" -v
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract ContextCompactionEngine strategy</name>
  <files>
    src/embedagent/strategies/context_compaction_engine.py
    src/embedagent/query_engine.py
    tests/test_strategies.py
  </files>
  <behavior>
    - Test: compact() reduces message list when over budget
    - Test: compact() returns messages unchanged when under budget
    - Test: _estimate_tokens() counts ~4 chars per token
  </behavior>
  <action>
Create `src/embedagent/strategies/context_compaction_engine.py` containing:

1. **Class `ContextCompactionEngine`** with constructor accepting:
   - `context_manager: ContextManager`
   - `max_tokens: int = 8000`
   - `reserve_tokens: int = 1000`

2. **Methods extracted from QueryEngine:**
   - `compact(messages: List[Dict[str, Any]]) -> List[Dict[str, Any]]` — lines ~1200-1350
   - `_estimate_tokens(messages: List[Dict[str, Any]]) -> int` — rough estimation
   - `_compact_by_truncation(messages: List[Dict[str, Any]], target_tokens: int) -> List[Dict[str, Any]]` — remove oldest messages
   - `_compact_by_summarization(messages: List[Dict[str, Any]], target_tokens: int) -> List[Dict[str, Any]]` — replace old turns with summary

3. **Implementation details:**
   - Use `context_manager.reducer_registry` if available for pluggable compaction strategies
   - Keep priority order: system message (never drop) > recent turns > old turns
   - `_estimate_tokens`: sum(len(msg.get("content", "")) for msg in messages) // 4
   - Truncation: remove messages from index 1 (after system) until under budget
   - Summarization: if truncation would remove >50% of messages, replace removed range with a summary message
   - Log compaction action at INFO level: "Compacted {before} -> {after} messages (~{tokens} tokens)"

4. **Update QueryEngine:**
   - Add import: `from embedagent.strategies.context_compaction_engine import ContextCompactionEngine`
   - In `__init__`, instantiate: `self._compaction = ContextCompactionEngine(context_manager, ...)`
   - Replace compaction logic in `_prepare_messages` with delegation

5. **Tests in tests/test_strategies.py`:**
   - `test_compact_reduces_over_budget`: messages totaling 20k tokens -> under 8k
   - `test_compact_noop_under_budget`: messages at 5k tokens -> unchanged
   - `test_estimate_tokens_approximation`: verify ~4 chars/token ratio

Run tests: `uv run pytest tests/test_strategies.py::TestContextCompactionEngine -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_strategies.py::TestContextCompactionEngine -v</automated>
  </verify>
  <done>
    - ContextCompactionEngine exists at src/embedagent/strategies/context_compaction_engine.py
    - QueryEngine._prepare_messages delegates to ContextCompactionEngine
    - All 3 characterization tests pass
    - Existing test suite still passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract TurnOrchestrator and finalize QueryEngine refactor</name>
  <files>
    src/embedagent/strategies/turn_orchestrator.py
    src/embedagent/strategies/__init__.py
    src/embedagent/query_engine.py
    tests/test_strategies.py
    tests/test_query_engine_orchestrator.py
  </files>
  <behavior>
    - Test: execute_turn() returns QueryTurnResult with reply and observations
    - Test: execute_turn() handles permission requests correctly
    - Test: execute_turn() handles tool execution errors gracefully
  </behavior>
  <action>
Create `src/embedagent/strategies/turn_orchestrator.py` containing:

1. **Class `TurnOrchestrator`** with constructor accepting:
   - `llm_wrapper: LLMClientRetryWrapper`
   - `tools: ToolRuntime`
   - `permission_policy: Optional[PermissionPolicy] = None`
   - `max_parallel_tools: int = 3`
   - `streaming_executor: Optional[StreamingToolExecutor] = None`

2. **Methods extracted from QueryEngine:**
   - `execute_turn(session: Session, messages: List[Dict[str, Any]], tools: List[Dict[str, Any]]) -> QueryTurnResult` — the main turn loop from lines ~800-1100
   - `_partition_and_execute_tools(session: Session, tool_actions: List[Action]) -> List[Observation]` — lines ~1100-1200
   - `_handle_permission_request(tool_call: Dict[str, Any]) -> Optional[PermissionRequest]` — lines ~1000-1050
   - `_build_observations(results: List[Any]) -> List[Observation]` — lines ~1150-1200

3. **Implementation details:**
   - Keep tool partitioning logic (parallel vs sequential based on write/read classification)
   - Keep permission check flow: permission_policy -> request -> wait/approve/deny
   - Keep observation enrichment (error details, truncation notices)
   - Maintain same `QueryTurnResult` structure
   - Keep streaming support through `streaming_executor`

4. **Update QueryEngine:**
   - Add import: `from embedagent.strategies.turn_orchestrator import TurnOrchestrator`
   - In `__init__`, instantiate: `self._turn_orchestrator = TurnOrchestrator(llm_wrapper, tools, ...)`
   - Replace `_execute_turn` body with delegation to `self._turn_orchestrator.execute_turn(session, messages, tools)`
   - Keep high-level `run()` method that manages multi-turn loop, memory maintenance, session persistence
   - QueryEngine becomes ~400 lines: constructor, `run()`, `stop()`, and a few helpers

5. **Create `src/embedagent/strategies/__init__.py`:**

```python
from embedagent.strategies.context_compaction_engine import ContextCompactionEngine
from embedagent.strategies.llm_retry_wrapper import LLMClientRetryWrapper
from embedagent.strategies.turn_orchestrator import TurnOrchestrator

__all__ = [
    "ContextCompactionEngine",
    "LLMClientRetryWrapper",
    "TurnOrchestrator",
]
```

6. **Tests in tests/test_strategies.py`:**
   - `test_execute_turn_returns_result`: mock LLM and tools, verify QueryTurnResult
   - `test_permission_request_handled`: mock permission_policy, verify PendingInteraction in result
   - `test_tool_error_observation`: mock tool raising ToolError, verify error observation

7. **Backward compatibility tests in tests/test_query_engine_orchestrator.py`:**
   - `test_query_engine_instantiation`: verify QueryEngine() still works with same args
   - `test_query_engine_run_signature`: verify run() accepts same parameters
   - `test_query_engine_stop_works`: verify stop() interrupts run()

Run all strategy tests: `uv run pytest tests/test_strategies.py tests/test_query_engine_orchestrator.py -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_strategies.py tests/test_query_engine_orchestrator.py -v</automated>
  </verify>
  <done>
    - TurnOrchestrator exists at src/embedagent/strategies/turn_orchestrator.py
    - src/embedagent/strategies/__init__.py exports all 3 strategies
    - QueryEngine is under 500 lines and retains run()/stop() public API
    - All 6 characterization tests pass (3 strategy + 3 compatibility)
    - Existing test suite still passes
    - No new deprecation warnings or bare except blocks introduced
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy -> Orchestrator | Internal code boundary; no untrusted input crosses here |
| Orchestrator -> External | Same as before; public API unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | LLMClientRetryWrapper | mitigate | Max retries bounded; exponential backoff prevents thundering herd |
| T-03-05 | Information Disclosure | ContextCompactionEngine | accept | Same token estimation as before; no new data exposure |
| T-03-06 | Tampering | TurnOrchestrator | mitigate | Tool execution still goes through official ToolRuntime facade |
</threat_model>

<verification>
1. Run full test suite: `uv run pytest tests/ -m "not slow and not gui" -v`
2. Verify QueryEngine line count: `wc -l src/embedagent/query_engine.py` (must be < 500)
3. Verify strategy files exist: `ls src/embedagent/strategies/*.py`
4. Verify no new deprecation warnings in test output
</verification>

<success_criteria>
- QueryEngine reduced from 1,530 lines to under 500 lines
- Three strategy modules exist with single, clear responsibilities
- All existing tests pass without modification
- New characterization tests cover strategy behavior
- QueryEngine public API (run(), stop(), constructor) unchanged
- No new deprecation warnings or bare except blocks introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture/03-02-SUMMARY.md`
</output>
