---
phase: 03-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/embedagent/inprocess_adapter.py
  - src/embedagent/services/session_lifecycle.py
  - src/embedagent/services/event_emitter.py
  - src/embedagent/services/workspace_file_service.py
  - src/embedagent/services/harness_state_synchronizer.py
  - src/embedagent/services/__init__.py
  - tests/test_services.py
  - tests/test_inprocess_adapter_facade.py
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "InProcessAdapter is under 500 lines (down from 2,446)"
    - "Four service modules exist with clear single responsibilities"
    - "All existing tests pass without modification"
    - "New characterization tests verify service behavior"
  artifacts:
    - path: "src/embedagent/services/session_lifecycle.py"
      provides: "Session creation, restoration, and teardown logic"
      min_lines: 100
    - path: "src/embedagent/services/event_emitter.py"
      provides: "Event serialization and broadcast to handlers"
      min_lines: 80
    - path: "src/embedagent/services/workspace_file_service.py"
      provides: "File operations within workspace boundaries"
      min_lines: 80
    - path: "src/embedagent/services/harness_state_synchronizer.py"
      provides: "Task graph refresh and harness state updates"
      min_lines: 60
    - path: "src/embedagent/services/__init__.py"
      provides: "Service exports"
    - path: "src/embedagent/inprocess_adapter.py"
      provides: "Thin facade delegating to services"
      max_lines: 500
  key_links:
    - from: "src/embedagent/inprocess_adapter.py"
      to: "src/embedagent/services/*"
      via: "Constructor injection"
      pattern: "from embedagent.services import"
---

<objective>
Extract InProcessAdapter (2,446 lines) into four focused services behind a thin facade.

Purpose: Reduce complexity, improve testability, and establish clear component boundaries
Output: Five new/modified files — four service modules + refactored facade
</objective>

<execution_context>
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Claude-project/ccode-win7/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/research/SUMMARY.md

Key patterns from codebase:
- InProcessAdapter at 2,446 lines mixes: session lifecycle, events, files, harness sync, commands, interactions
- Uses `with state.lock:` pattern extensively (30+ occurrences)
- Constructor takes: session_store, timeline_store, summary_store, plan_store, project_memory, tool_runtime, permission_policy
- Must maintain backward compatibility — other modules import InProcessAdapter directly
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract SessionLifecycle service from InProcessAdapter</name>
  <files>
    src/embedagent/services/session_lifecycle.py
    src/embedagent/inprocess_adapter.py
    tests/test_services.py
  </files>
  <behavior>
    - Test: create_session() returns ManagedSession with correct initial state
    - Test: restore_session() loads from store and rebuilds transcript
    - Test: teardown_session() persists summary and clears runtime
  </behavior>
  <action>
Create `src/embedagent/services/session_lifecycle.py` containing:

1. **Class `SessionLifecycleManager`** with constructor accepting:
   - `session_store: SessionSummaryStore`
   - `timeline_store: SessionTimelineStore`
   - `summary_store: SessionSummaryStore` (may be same as session_store)
   - `plan_store: PlanStore`
   - `project_memory: ProjectMemoryStore`
   - `session_restorer: SessionRestorer`
   - `transcript_store: TranscriptStore`

2. **Methods extracted from InProcessAdapter:**
   - `create_session(project_path: str, mode: str) -> ManagedSession` — lines ~180-280 of current inprocess_adapter.py
   - `restore_session(session_id: str) -> Optional[ManagedSession]` — lines ~290-400
   - `teardown_session(session: ManagedSession) -> None` — lines ~2100-2200
   - `list_sessions() -> List[Dict[str, Any]]` — lines ~2200-2250

3. **Preserve behavior exactly:**
   - Keep `with state.lock:` pattern inside methods that access ManagedSession state
   - Keep `_normalize_recent_transitions` helper (move to module level or keep inline)
   - Keep `_utc_now()` helper (import from a shared location or duplicate)
   - Maintain same error handling (log and return None on failure, not raise)

4. **Update InProcessAdapter:**
   - Add import: `from embedagent.services.session_lifecycle import SessionLifecycleManager`
   - In `__init__`, instantiate: `self._session_lifecycle = SessionLifecycleManager(...)`
   - Replace `create_session` body with: `return self._session_lifecycle.create_session(project_path, mode)`
   - Replace `restore_session` body with: `return self._session_lifecycle.restore_session(session_id)`
   - Replace `teardown_session` body with: `self._session_lifecycle.teardown_session(session)`
   - Replace `list_sessions` body with: `return self._session_lifecycle.list_sessions()`

5. **Tests in tests/test_services.py:**
   - `test_create_session_initializes_correctly`: verify session has id, project_path, mode, empty turns
   - `test_restore_session_rebuilds_transcript`: mock stores, verify transcript events replayed
   - `test_teardown_persists_summary`: verify summary_store.save called with session data
   - `test_list_sessions_returns_dicts`: verify format matches existing behavior

Run tests: `uv run pytest tests/test_services.py::TestSessionLifecycle -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_services.py::TestSessionLifecycle -v</automated>
  </verify>
  <done>
    - SessionLifecycleManager exists at src/embedagent/services/session_lifecycle.py
    - InProcessAdapter.create_session delegates to SessionLifecycleManager
    - All 4 characterization tests pass
    - Existing test suite still passes: uv run pytest tests/ -m "not slow and not gui" -v
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract EventEmitter service</name>
  <files>
    src/embedagent/services/event_emitter.py
    src/embedagent/inprocess_adapter.py
    tests/test_services.py
  </files>
  <behavior>
    - Test: emit() calls all registered handlers with correct (event_type, session_id, payload)
    - Test: handler exceptions do not prevent other handlers from running
    - Test: add_handler() and remove_handler() work correctly
  </behavior>
  <action>
Create `src/embedagent/services/event_emitter.py` containing:

1. **Class `EventEmitter`** with:
   - Constructor takes no dependencies
   - `_handlers: Dict[str, List[EventHandler]]` — event_type -> handlers
   - `_global_handlers: List[EventHandler]` — handlers receiving all events

2. **Methods extracted from InProcessAdapter:**
   - `add_handler(event_type: Optional[str], handler: EventHandler) -> None`
   - `remove_handler(event_type: Optional[str], handler: EventHandler) -> None`
   - `emit(event_type: str, session_id: str, payload: Dict[str, Any]) -> None` — lines ~450-550 of inprocess_adapter.py

3. **Implementation details:**
   - Keep threading-safe behavior: copy handler list before iterating
   - Catch and log exceptions in individual handlers (do not let one handler crash others)
   - Maintain exact event types: "session_created", "turn_started", "turn_completed", "session_closed", etc.
   - Keep serialization logic for payload (convert dataclasses to dict, handle datetime)

4. **Update InProcessAdapter:**
   - Add import: `from embedagent.services.event_emitter import EventEmitter`
   - In `__init__`, instantiate: `self._event_emitter = EventEmitter()`
   - Replace `add_event_handler` with: `self._event_emitter.add_handler(event_type, handler)`
   - Replace `remove_event_handler` with: `self._event_emitter.remove_handler(event_type, handler)`
   - Replace `_emit_event` internal method with: `self._event_emitter.emit(event_type, session_id, payload)`

5. **Tests in tests/test_services.py:**
   - `test_emit_calls_registered_handlers`: register 2 handlers, emit, both called with correct args
   - `test_handler_exception_isolated`: handler1 raises, handler2 still called
   - `test_add_remove_handler`: add, remove, verify not called after removal

Run tests: `uv run pytest tests/test_services.py::TestEventEmitter -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_services.py::TestEventEmitter -v</automated>
  </verify>
  <done>
    - EventEmitter exists at src/embedagent/services/event_emitter.py
    - InProcessAdapter._emit_event delegates to EventEmitter
    - All 3 characterization tests pass
    - Existing test suite still passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract WorkspaceFileService and HarnessStateSynchronizer</name>
  <files>
    src/embedagent/services/workspace_file_service.py
    src/embedagent/services/harness_state_synchronizer.py
    src/embedagent/services/__init__.py
    src/embedagent/inprocess_adapter.py
    tests/test_services.py
  </files>
  <behavior>
    - Test: WorkspaceFileService.resolve_path enforces workspace boundary
    - Test: WorkspaceFileService.read_file returns content for paths within workspace
    - Test: HarnessStateSynchronizer.refresh_task_graph updates session snapshot
    - Test: HarnessStateSynchronizer.sync_mode updates mode in session state
  </behavior>
  <action>
Create two service modules and update the facade:

**A. WorkspaceFileService (`src/embedagent/services/workspace_file_service.py`):**

1. **Class `WorkspaceFileService`** with constructor accepting:
   - `workspace_root: str`

2. **Methods extracted from InProcessAdapter:**
   - `resolve_path(relative_path: str) -> str` — resolves and validates within workspace
   - `read_file(path: str) -> str` — read with encoding fallback
   - `write_file(path: str, content: str) -> None` — atomic write
   - `list_directory(path: str) -> List[Dict[str, Any]]` — directory listing
   - `glob_files(pattern: str) -> List[str]` — glob within workspace

3. **Implementation details:**
   - Keep path traversal protection (reject `..` that escapes workspace)
   - Keep encoding detection logic (utf-8 -> utf-8-sig -> latin-1 fallback)
   - Keep `SKIP_DIR_NAMES` filtering
   - Log operations at DEBUG level

4. **Update InProcessAdapter:**
   - Add import: `from embedagent.services.workspace_file_service import WorkspaceFileService`
   - In `__init__`, instantiate: `self._workspace_files = WorkspaceFileService(workspace_root)`
   - Replace file operation methods with delegation to `self._workspace_files`

**B. HarnessStateSynchronizer (`src/embedagent/services/harness_state_synchronizer.py`):**

1. **Class `HarnessStateSynchronizer`** with constructor accepting:
   - `harness_runner: HarnessRunner`
   - `task_store: Any` (the task_store module or object)

2. **Methods extracted from InProcessAdapter:**
   - `refresh_task_graph(session: ManagedSession) -> None` — lines ~1800-1900
   - `sync_mode(session: ManagedSession, mode: str) -> None` — lines ~1900-1950
   - `build_snapshot(session: ManagedSession) -> Dict[str, Any]` — lines ~1950-2050

3. **Implementation details:**
   - Keep `with state.lock:` when accessing session
   - Keep interaction with `HarnessRunner` and `task_store` exactly as before
   - Maintain snapshot structure (tasks, mode, plan, phase, etc.)

4. **Update InProcessAdapter:**
   - Add import: `from embedagent.services.harness_state_synchronizer import HarnessStateSynchronizer`
   - In `__init__`, instantiate: `self._harness_sync = HarnessStateSynchronizer(harness_runner, task_store)`
   - Replace harness sync methods with delegation

**C. Create `src/embedagent/services/__init__.py`:**

```python
from embedagent.services.event_emitter import EventEmitter
from embedagent.services.harness_state_synchronizer import HarnessStateSynchronizer
from embedagent.services.session_lifecycle import SessionLifecycleManager
from embedagent.services.workspace_file_service import WorkspaceFileService

__all__ = [
    "EventEmitter",
    "HarnessStateSynchronizer",
    "SessionLifecycleManager",
    "WorkspaceFileService",
]
```

**D. Tests in tests/test_services.py:**
- `TestWorkspaceFileService`:
  - `test_resolve_path_enforces_boundary`: `../../../etc/passwd` rejected
  - `test_read_file_returns_content`: write temp file, read back
  - `test_list_directory_filters_skip_dirs`: verify `.git` excluded
- `TestHarnessStateSynchronizer`:
  - `test_refresh_updates_snapshot`: mock harness_runner, verify snapshot updated
  - `test_sync_mode_changes_mode`: verify session state updated

Run tests: `uv run pytest tests/test_services.py -v`
</action>
  <verify>
    <automated>uv run pytest tests/test_services.py -v</automated>
  </verify>
  <done>
    - WorkspaceFileService and HarnessStateSynchronizer exist
    - src/embedagent/services/__init__.py exports all 4 services
    - InProcessAdapter delegates to both services
    - All characterization tests pass (7 tests total across both services)
    - Existing test suite still passes
    - InProcessAdapter is under 500 lines
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> Adapter | Internal code boundary; no untrusted input crosses here |
| Adapter -> External | Same as before; facade does not expand attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | WorkspaceFileService | mitigate | Keep path traversal checks; no relaxation of boundary enforcement |
| T-03-02 | Denial of Service | EventEmitter | accept | Handler exceptions are already isolated; no change to behavior |
| T-03-03 | Elevation of Privilege | SessionLifecycleManager | mitigate | Same lock semantics as before; no new capabilities exposed |
</threat_model>

<verification>
1. Run full test suite: `uv run pytest tests/ -m "not slow and not gui" -v`
2. Verify InProcessAdapter line count: `wc -l src/embedagent/inprocess_adapter.py` (must be < 500)
3. Verify service files exist: `ls src/embedagent/services/*.py`
4. Verify no new deprecation warnings in test output
</verification>

<success_criteria>
- InProcessAdapter reduced from 2,446 lines to under 500 lines
- Four service modules exist with single, clear responsibilities
- All existing tests pass without modification
- New characterization tests cover service behavior
- No new deprecation warnings or bare except blocks introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture/03-01-SUMMARY.md`
</output>
